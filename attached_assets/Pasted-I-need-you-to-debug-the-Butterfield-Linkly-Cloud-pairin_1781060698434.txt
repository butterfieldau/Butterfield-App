I need you to debug the Butterfield Linkly Cloud pairing failure on the API server.

Context:
- The Linkly settings now save successfully.
- The app shows:
  - Configured: Ready
  - Paired: Not paired
  - Token: No active token
- When pressing "Pair PIN Pad", the UI shows:
  - "Pairing Failed"
  - "Linkly pairing failed."
- This happens in both sandbox and production.
- The app is running from an Expo dev session, but the API is running on Replit.
- The latest code with Linkly Cloud integration is already in Git and pushed.
- We already fixed one backend schema issue around Linkly default values.

What I need you to do:
1. Inspect the API server logs for the Linkly pairing request.
2. Find the exact backend error being thrown when POST /pos/linkly/pair is called.
3. Confirm whether the request is reaching the Linkly auth endpoint successfully or failing before that.
4. Log the response status and response body from Linkly pairing without exposing secrets.
5. Confirm whether the saved Linkly config includes:
   - environment
   - username
   - password exists
   - pair code
   - posName
   - posVersion
   - posId
   - posVendorId
6. Check whether posVendorId is being rejected because it is not a UUID.
7. Check whether the pair code is expired or invalid.
8. Check whether sandbox credentials are being used against production or vice versa.
9. Tell me exactly which of these is failing:
   - app -> API route
   - API -> config load
   - API -> Linkly auth request
   - Linkly response validation
   - secret storage after successful pair

Important:
- Do not print username, password, token, or secret values in logs.
- You may print whether each value exists and whether UUID fields are valid.
- If needed, temporarily improve the pairing error message returned by the API so the UI shows the real failure reason instead of only “Linkly pairing failed.”

Please return:
- the exact failing step
- the exact error message from the backend
- whether the POS Vendor ID and POS ID are valid UUIDs
- whether the pair code looks valid/rejected
- the exact fix needed